services:
  - type: web
    name: jarvis-ai
    runtime: docker
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: PORT
        value: 8000
    plan: free
    healthCheckPath: /health
